var Wordynator = (function(module){

    var id = "Wordynator";

    function load(){}
    function reload(){}

    // exports 
    module.load = load;
    module.reload = reload;
    module.search = {};

    return module;

}(Wordynator || {}));